## Domain docs

This repo documents its domain language and architectural decisions for agents. Before working in the domain, follow
`docs/agents/domain.md`: read `LANGUAGE.md` (or `CONTEXT-MAP.md`) and the relevant ADRs in `docs/adr/`, and use the
glossary's vocabulary. If those files don't exist, proceed silently.
